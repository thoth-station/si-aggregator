- project:
    check:
      jobs:
        - "thoth-pytest"
    gate:
      queue: "CHANGEME"
      jobs:
        - "thoth-pytest"
    post:
      jobs:
        - "trigger-build":
            vars:
              cluster: "paas.psi.redhat.com"
              namespace: "thoth-middletier"
              buildConfigName: "si-aggregator"

    kebechet-auto-gate:
      queue: "thoth-station/core"
      jobs:
        - "thoth-pytest"
